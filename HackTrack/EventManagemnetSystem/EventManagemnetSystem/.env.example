# Backend Environment Variables

# Database connection (MySQL)
DB_HOST=your-database-host.example.com
DB_PORT=3306
DB_USER=your-database-user
DB_PASSWORD=your-database-password
DB_NAME=event_management

# Database SSL (optional - set to 'true' to enable)
DB_SSL_ENABLED=false
DB_SSL_CA_PATH=/path/to/ca.pem

# Auth / JWT
JWT_SECRET=your-jwt-secret-key-here
JWT_EXPIRES_IN=1h

# Frontend URL (for CORS)
FRONTEND_URL=https://your-frontend.vercel.app

# Rate Limiting (for serverless endpoints)
# Note: These limits are per-instance only. For production, consider using a shared store like Upstash Redis.
RATE_LIMIT_WINDOW_MS=60000  # Time window in milliseconds (default: 60000 = 1 minute)
RATE_LIMIT_MAX=60           # Maximum requests per window (default: 60)
