# Serverless Backend Configuration
# This backend is deployed as Vercel Edge Functions
# All endpoints are in the /api directory and deployed to Vercel

# Database connection (MySQL/Aiven)
DB_HOST=your-database-host.example.com
DB_PORT=3306
DB_USER=your-database-user
DB_PASSWORD=your-database-password
DB_NAME=event_management

# Database SSL (Aiven requires SSL - keep enabled for production)
DB_SSL_ENABLED=true
DB_SSL_CA_PATH=./ca.pem

# Authentication (JWT)
JWT_SECRET=your-jwt-secret-key-here-min-32-chars-recommended
JWT_EXPIRES_IN=1h

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
# Production: FRONTEND_URL=https://your-frontend.vercel.app

# Rate Limiting
# These limits are per-instance. For production, use Upstash Redis (optional)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=60
NODE_ENV=production
